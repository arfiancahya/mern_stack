import React, { Fragment } from 'react';

const Gap = ({width, height}) => {
    return (
        <Fragment>
            <div style={{width, height}} />
        </Fragment>
    );
}

export default Gap;
